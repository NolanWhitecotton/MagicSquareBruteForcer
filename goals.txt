Dynamic Insertion (DI)
	Add a method to define the Square::Add order
	Add a method to define the Sqiare::removeLastAdd order/checks
	Make isValid work with the Square::Add order
	Multiple Insertion (MI)
		insert multiple things then check them all

Progress Reports (PR)
	
Fork at depth (FK)
	Fork after a certain depth, and use this in the thread allocation and/or dynamically

Print the steps its going through as it does them with -v enabled

output manager
	allow a tee to a file for output
	show warnings/thread updates
